/**
 * @deprecated Import from 'src/cache/index.js' directly.
 * This re-export exists for backward compatibility with any code that
 * still references the old path. It will be removed in a future release.
 */
export * from '../cache/index.js';
